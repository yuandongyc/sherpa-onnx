name: A FFmpeg Tests

on:
  push:
    branches:
      - master
      - github-actions
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  ffmpeg-tests:
    name: FFmpeg windows-latest
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ffmpeg
            mingw-w64-x86_64-wget
            mingw-w64-x86_64-curl
            make

      - name: Build
        shell: msys2 {0}
        run: |
          mkdir build
          cd build
          cmake \
            -G "MinGW Makefiles" \
            -DCMAKE_BUILD_TYPE=Release \
            -DSHERPA_ONNX_ENABLE_PYTHON=OFF \
            -DSHERPA_ONNX_ENABLE_C_API=ON \
            -DSHERPA_ONNX_ENABLE_PORTAUDIO=OFF \
            ..
          mingw32-make -j2

          # Build ffmpeg example
          cd ffmpeg-examples
          make

      - name: Download test model
        shell: msys2 {0}
        run: |
          cd ffmpeg-examples
          curl -SL -O https://github.com/k2-fsa/sherpa-onnx/releases/download/asr-models/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20.tar.bz2
          tar xvf sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20.tar.bz2
          rm sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20.tar.bz2

      - name: Test with greedy_search
        shell: msys2 {0}
        run: |
          cd ffmpeg-examples/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20
          ../build/ffmpeg-examples/sherpa-onnx-ffmpeg.exe \
            ./tokens.txt \
            ./encoder.onnx ./decoder.onnx ./joiner.onnx \
            ./test_wavs/0.wav \
            2 \
            greedy_search

      - name: Test with modified_beam_search
        shell: msys2 {0}
        run: |
          cd ffmpeg-examples/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20
          ../build/ffmpeg-examples/sherpa-onnx-ffmpeg.exe \
            ./tokens.txt \
            ./encoder.onnx ./decoder.onnx ./joiner.onnx \
            ./test_wavs/0.wav \
            2 \
            modified_beam_search

      - name: Test with URL (network stream)
        shell: msys2 {0}
        run: |
          cd ffmpeg-examples/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20
          ../build/ffmpeg-examples/sherpa-onnx-ffmpeg.exe \
            ./tokens.txt \
            ./encoder.onnx ./decoder.onnx ./joiner.onnx \
            https://huggingface.co/csukuangfj/sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20/resolve/main/test_wavs/3.wav \
            2 \
            greedy_search

      - name: Cleanup
        if: always()
        shell: msys2 {0}
        run: |
          cd ffmpeg-examples
          rm -rf sherpa-onnx-streaming-zipformer-bilingual-zh-en-2023-02-20
